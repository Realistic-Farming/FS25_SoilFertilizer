# Roadmap: FS25_SoilFertilizer

> Ecosystem role: **Soil and Crops** · Part of the Realistic Farming connected suite
> Status: FILLED from the ecosystem audit/baseline.
> Forward-looking only. Shipped history lives in CHANGELOG.md and the releases.

## How to use this file
- Populate the milestones below from the audit baseline once it lands.
- Each item should be small enough to map to a `TODO.md` entry.
- Keep it honest: near-term is committed, mid-term is intended, long-term is aspirational.

## Current baseline
- Version at baseline: v2.4.7.0 (development); the pre-release testing cycle for the disease cluster + physical fungicides + the Refined engine adoption.
- Refined per-pixel value-map engine (WizardlyPayload's #736) adopted and merged into development (cd871bd7); our simulation runs on top of it. The old SoilLayerInstaller handshake is no longer required.
- Audit reference: ecosystem-dev-tracking Point 1-8 docs (baseline v3); CLAUDE-LOG.md through the 2026-07-24 spatial-soil drop
- Baseline date: 2026-07-08 (updated 2026-07-25)

## Near-term (next release cycle)
- [x] F165 growth-credit store invalidation (2026-08-12): the SF-53 store now has an invalidation path. A nil stored fruitIndex is a reset, not a pass, and a cell reading fruit UNKNOWN at the bell is cleared. Closes the fallow-bank exploit and its two costumes. SF-53 spec bar at 61 assertions.
- [x] SF-76 field genesis (2026-08-12): a new save's starting soil is seeded FROM TERRAIN instead of the smooth regional gradient. `_genesisDeviation` (height-relative, slope, sink proximity from SF-77's TopographyCache when present, one direct sample when not) replaces the regional term in `_computeInitialSoil` only when `genesisActive` is set, with same amplitude bounds, noise and clamps untouched. The manager arms genesis on a NEW save (no soilData.xml yet, server-only) with a deterministic per-save seed (savegame directory hash: same seed twice = identical soil; an existing save is untouched, zero writes). Deterministic, never math.randomseed. 23 assertions in SF-76-field_genesis_spec_test.lua.
- [x] SF-77 topography cache (2026-08-12): `TopographyCache.lua`, the load-time terrain grid. Adaptive grid (12-48 m by map size, floor rounding, 180k hard cap) built once at map load, row-major, carrying per-cell height, slope class, sink data and (ROUND-2) distance-to-water from the engine height field and the water plane. Terrain edits mark cells STALE (via the terrainDeformationSyncer listener); stale answers are shaped defaults, never nil. The static water-dist table persists through its own StateLedger module and delivers via a NetworkSync module (dirty only on terrain invalidation). Consumers: SF-76 first, SCS-042 second. Neutral until a consumer wires in. 54 assertions in SF-77-topography_cache_spec_test.lua.
- [x] Spatial scouting walked mask (SF-26): on-foot walking reveals the trouble's pattern where you walked, per farm, fading after N in-game days. Server-authoritative sampling, per-farm storage in its own home, Time Guard aging, StateLedger persistence, NetworkSync delivery (each payload entry carries its own truth), and a read-time compose on the disease map that never touches the shared mirrors. **Merged to main in PR #774.** Reading surface and the kneel/handful members build on top.
- [x] The handful read (SF-38): the frozen payload contract the Read the Dirt panel renders. One kneel, one payload, assembled from getters that all already ship, each clause carrying its honest grain (spot/cell/field) and its own gate. Pure assembly, zero writes; the material verdict takes the fill type from the caller because the ground-material layers are material-blind; diseaseKnown is cell-grain via the walked mask when a fresh cell exists. 35 assertions. **Merged to main in PR #774.** The kneel (SF-37) calls it after its reveal write.
- [x] The kneel (SF-37): the active, precise reveal verb. Kneel (Shift+K) at a spot and the exact cell enters knowledge: one server-authoritative write onto the walked mask, LAW 4 (the client key press is a request carrying only x,z; the farm resolves from the requesting player's record via the connection). The field-level scout fee path is untouched for spotless callers. 11 assertions. **Merged to main in PR #775.**
- [x] Variable pest and disease pressure (SF-19): outbreaks start in a patch and grow, instead of one number covering a whole field. `SpatialPressures.lua` adds ORIGIN (disease weights per-cell soil incl. compaction, pest weights field edge) and SPREAD (seed and stamp, single hop per day, anti-saturation as exclusion) on top of the untouched daily field model. Its hard gate (per-cell store coherence, SF-13) is cleared. 22 assertions. PR #776 open, awaiting merge.
- [x] Release gate (2026-08-02): stable-vs-experimental lock, orthogonal to difficulty. `ReleaseGate.lua` carries Arissani's certified first lock set; the explicit opt-in is `Settings:allowsExperimentalSystems()`, independent of difficulty. Experimental console commands refuse when locked (mirrors bypassLockedMsg). **Sim wiring included:** the locked systems' entry points are gated (ground-material + Read the Dirt arm only when live, SF-19 daily run gated, CD-9/10/12 fungicide+hybrid paths gated, bridge registration gated). This is the instrument that lets a stable release ship the proven baseline + bug fixes while the new wave stays inert until deliberately released.
- [x] Oilseed-radish nitrate by direct drill (issue #778): the crop-incorporation probe is now installed on `SowingMachine`, and `onSowing` accepts a `cropBiomass` argument to award the new `CROP_INCORPORATION.SOWING` profile (OM 0.4 / N 2.0 / P 0.4 / K 1.2, calibrated between the mulcher and the flat residue) on top of the flat DIRECT_DRILL residue, gated on `residueIncorporation` and biomass > 0. A direct drill that terminates a standing/dead cover crop now releases a visible green-manure N boost instead of reading "unchanged". PR open, awaiting merge.
- [ ] Adopt NetworkSync v2 sub-module delta: add `onWriteDelta`/`onReadDelta` to SoilNetworkSyncBridge so only changed fields sync instead of the whole field map.
- [!] 255 fill type cap (issue #755): Giants Engine hard cap, not our bug. 25 custom fill types + base game + DLC can exceed the limit. Design options: ship as-is, consolidate fill types, or degrade gracefully. See ecosystem ledger 2026-07-26 for recommendation (hybrid approach).
- [~] Two-machine MP verification of all four bedrock bridges: reframed 2026-07-15 - the live two-machine test is out of scope (no dedicated-server budget). A single-machine network round-trip harness now covers serialization desync for every event/bridge; single-host smoke on top. Registration already confirmed in-game.
- [ ] Lock the provisional module ids with Claude(A): `SoilFertilizer_Soil` (StateLedger) and `SoilFertilizer_Sync` (NetworkSync) before they ship in a release.

## Near-term (2026-08-05 wave)
- [x] SF-53 growth credit (the SF-2M reward half, ratified 2026-08-12): `GrowthCredit.lua`, a daily Time Guard bookkeeper (priority 97) counts days of excellence per survey cell; the period hand advances credit cells one step at the drained FINISHED_GROWTH_PERIOD delivery, bucketed by (fruit, current, target) with one BETWEEN-filtered executeSet per bucket on the engine's own fruit plane. Engine-true targets from the crop's growthMapping (seasonal periods or daily), skip-at-own-cutState, never into cut/withered/max, capture-and-restore for blocked cells. Inert behind the growth_modulation release gate and SF-52's mask enable. 54 assertions in SF-53-growth_credit_bucket_spec_test.lua. Unlock gated on SF-54's reading surface.
- [x] SF-78 growth block (the SF-2M hold half, ruled OPEN and STANDALONE, ratified 2026-08-12): `GrowthBlock.lua`, capture at START_GROWTH_PERIOD (write-once across a bracket), restore at the drained FINISHED delivery through the same write machine. R2 three-halves discriminator (fruit unchanged, not cut/withered, strictly above captured); target max(captured, current - cap); the capture is cleared UNCONDITIONALLY at every drained delivery (cert assertion). No Time Guard registration; the engine's own bracket is the clock. Inert behind the growth_modulation release gate and SF-52's mask enable. 24 assertions in SF-78-growth_block_restore_spec_test.lua. Unlock gated on SF-54's reading surface.
- [~] Text fitting for the raw renderText surfaces (SF #771, Arissani shape ruling 2026-08-05): `UIHelper.fitText` and `UIHelper.renderTextFitted` port the base game's own TextElement RESIZE policy (`gui/elements/TextElement.lua:479-490`, verified firsthand): shrink 5 percent of the requested size per step, floor at 70 percent, and only then truncate with an ellipsis via `Utils.limitTextToWidth` (`utils/Utils.lua:16`, signature verified). Wired through `SoilSettingsPanel:drawText`, the single funnel every draw in that panel already used, with `maxWidth` optional so existing callers are untouched. 38 assertions. **PARTIAL:** the helper exists and the settings panel is wired; `SoilHUD.lua` has 31 raw `renderText` calls that still need per-column widths, and eight other `src/ui/` files another 39 between them. Built on development, PR open.
- [x] Dry products are haulable (SF #773, Arissani ruled PARITY 2026-08-05): our twelve dry products join `BULK` and `AUGERWAGON`, the two transport categories vanilla FERTILIZER is already in. The gap was never the products: each already declares `isBulkType="true"`, which per `FillTypeDesc.lua:287` only governs being SOLD as bulk. We had extended the APPLICATION categories (FERTILIZER, LIQUIDFERTILIZER) and never a TRANSPORT one, so a player could spread urea and could not tip it. Category extension confirmed additive at source (`FillTypeManager.lua:145` reuses an existing index when `isBaseType` is false, and mods load with false at `:85`), so nothing is displaced from BULK. The liquid half of the parity needs no change: vanilla LIQUIDFERTILIZER is in SPRAYER only and never in LIQUID, so a liquid rides the machine that applies it, which our LIQUIDFERTILIZER line already covers. Built on development, PR open.
- [x] Organic market premium provenance (OM-213, SF half): `OrganicCertification.organicFraction[farmId][fillTypeIndex]` accumulates the farm-level organic share of each harvested fill type with the D1 blend, folded per harvest pass from the combine's own `farmId`/`outputFillType` (dedi-safe, never the local-player read). Publishes `getFarmOrganicFraction(farmId, fillTypeIndex)` for MarketDynamics' OrganicPremium modifier. Persists via soilData.xml (safety copy) and the StateLedger block. Ships LOCKED with the premium; unlock gated on the FarmTablet market-report line (Wizard lane). Built on development, PR open.
- [x] Spray-paint streak re-fix (RSF-762, fast track): `SoilValueMaps:addPaintStrip` is a new additive parallelogram painter; `paintBoomStrip` is reimplemented as a swept quad (previous painted boom line to current line) that never overlaps, self-heals a failed tick by spanning the gap, and conserves the tick's nutrient mass with the strip's own area as denominator. `markBoomCells` is now coverage-only. 34 assertions across the rewritten #735 test and the new RSF-762 bar. Built on development, PR open.

## Mid-term (this season)
- [ ] ProStaff fertilizer discount bridge (silent, pcall-guarded read of `getFertilizerDiscount`) once scheduled. Not built today by decision (Point 8).
- [x] Grass drying / rotting (issue #749): shipped as the ground-material family (SF-43 to SF-49, IDs SF-42 superseded). MATERIAL DOWN records how long material lies out, WHAT THE SKY DID carries wetness and the water record, THE HAY BET converts by condition, STRAW DOWN covers the combine swath, THE YARD LADDER handles bale condition. Built on `development` and merged to main in PR #767. Reading surface (SF-48, Wizard lane) still deferred by ruling.
- [ ] Decide whether `getFieldInfo` should expose FieldSentry state (isSleeping/isMeadow/contractMaskActive) instead of companions reading `g_currentMission.fieldSentry` directly.
- [ ] Emit a clean disease-at-harvest signal (pathogen id + pressure) for an external diseased-food/mycotoxin model. Data already retained on fieldData at harvest; expose it deliberately.

## Long-term / aspirational
- [~] Deeper agronomy passes as the sim matures (compaction, cover, rotation tuning) without breaking the one-yield-truth pillar. Progress: no-till OM gradient (#738) and the rotation planner (#739) landed this cycle; compaction and cover remain.
- [x] Per-pixel value-map parity: shipped via the Refined engine adoption (#736). The SoilLayerInstaller handshake is retired (the installer is no longer required).

## Cross-mod / ecosystem dependencies
- [ ] NetworkSync v2 delta adoption (blocks on: FS25_NetworkSync v2.0.0.0, now released; this is an opt-in on our side).
- [ ] getFieldInfo contract confirmation (blocks on: CropDisease and DairyCore audits confirming they call the API, not the internal table).
- [ ] ProStaff discount (blocks on: FS25_ProStaffCoOp `proStaffManager` handle + the SF-side cost hook site being scheduled).
- [ ] Soil moisture coupling: expose SF's per-field compaction + organic matter as a water-retention signal for SeasonalCropStress to modulate moisture stress (compaction sharpens wet/dry, OM buffers). PIPELINE, community-originated (nemrod153). Arrow-ownership call ANSWERED 2026-07-15 (Claude(A)): Option B + firewall - SCS stays moisture authority, SF read-only one-way, the loop is cut by single-writer discipline (matches SF's lean). SF's export half is now buildable; the SCS consumption half folds into the SCS #89 rebuild. Proposal: ecosystem-dev-tracking `systems/soil-moisture-coupling/README.md`.

## Deferred / parked
- Precision Farming integration: never. Permanent stand-down house rule, not a roadmap item.
- Grass FIELD crop parity (weed/pest/disease + OM/organic + yield-% for grass grown on a real field): SCOPED IN 2026-07-16 (was "parked by design"). New rule: meadow grass stays soil-aware only (out of those models), but grass on a managed FIELD participates like any crop. Build surface: re-gate the `not isGrass` skips (SoilFertilitySystem.lua:269 weed / 287 pest / ~324 disease) on meadow-vs-field using the FieldSentry `isMeadow` discriminator (SoilFertilitySystem.lua:3016) instead of fruit type, and give grass fruit types disease-susceptibility + yield-% values (balance pass). Feeds Feed Provenance (a grass FIELD then seeds disease automatically via the harvest bus). Meadow grass (no fieldId) stays neutral and out.
- Rotation Foresight v2: an at-the-drill / sowing pre-plant prompt. Needs a reachable sowing/pre-plant hook. v1 SHIPPED this cycle on the field-detail / scout / FarmTablet surface (#739 data surface + the in-menu rotation planner dialog #744 + the FarmTablet app); v2 (the sowing-time prompt) still needs the hook. (ledger OPEN A)
- Disease progressive reveal: a richer readout where the Disease track mirrors the intel ladder - blank unscouted, "present" once the dog flags it, full % + name once scouted. First half SHIPPED: the unscouted indicator (cb29b018) reads UNKNOWN instead of clean green. The "present once the dog flags it" middle rung and full per-field knowledge state in SoilHUD remain. (ledger, 2026-07)
- #740 World Climate selection UI: the player-facing climate picker for the short-month rain fill is in review as Wizard PR #747 (renames the control to World Climate, 4-chip picker, 26 languages). Merge lands the UI on the already-built mechanism.


## 2026-08-06 (Fred): Esc RF dialogs + map buttons reachable again
- [x] With the RF Esc door live, the legacy menuSoilFertilizer Esc page is stood down, which also nilled inGameMenu[pageName]. That killed three openers: the Rotation Planner dialog, the per-field detail dialog, and the map sidebar report/treatment buttons (they read the nil page and returned silently).
- [x] Rotation Planner and Field Detail now open from the Esc panel bottom bar (MENU_EXTRA_2 / MENU_ACTIVATE), passing the panel's selectedFieldId (nil allowed).
- [x] The map sidebar report/treatment buttons were restored via the retained-page pattern (stand-down keeps the deep page on SoilPDAScreen._retainedDeepScreen). That door was then retired on 2026-08-12: the sidebar now draws only Disable Overlay and Help, the report/treatment buttons and their click handlers are gone, and the deep screen stays reachable from the Esc panel. See the 2026-08-12 entry below.

## 2026-08-12 (Fred): map sidebar trimmed to Disable Overlay + Help
- [x] The soil layer tab sidebar drew four action buttons (Farm Overview, Treatment Plan, Disable Overlay, Help). The first two were redundant doors to the deep PDA screen and are removed: the sidebar now draws only Disable Overlay and Help, and the dead report/treatment click branches in onSideBarClick are gone. The deep screen remains reachable from the Esc panel (Rotation Planner and Field Detail buttons). Untested in-game.

## 2026-08-12 (Fred): map sidebar footer - health summary aligned + per-layer info box
- [x] The Average Soil Health summary was anchored to the map footer buttons, so it sat well below the layer buttons. It now stacks directly under the column content (below the legend when a layer is active), so it aligns with the sidebar. In-game observation pending.
- [x] A small black info box (matching Wizard's RF_InfoBoxBg style) now renders under the health summary when a layer is selected, showing the layer name plus a one-line explanation. Twelve new sf_map_layer_desc_* keys ship across all 27 translation files. Untested in-game.

## 2026-08-07 (Fred): module page dots always visible
- [x] The Esc RF module selector hid its page dots when Worker Costs or Market Dynamics was the active module. Soil and Crop Stress always showed theirs, so WC never read as the 3rd module and the left panel was inconsistent. All four RfPdaMenuPage copies now keep the dots visible (dots = N, chrome unchanged, per the esc-rf-pda umbrella brief). Built, deployed, PR open.


## 2026-08-07 (Bob): SF #764 root-caused and fixed - AI out-of-fill stop restored
- [x] Issue #764 (Courseplay empty-tank): the diagnostic 2.5.0.6 trace build proved the tank reaches exactly zero in the failing runs but the fill unit type does NOT reset to UNKNOWN, so Sprayer:processSprayerArea never raises stopCurrentAIJob(OutOfFill) and Courseplay keeps driving. Root cause: the final drain leaves a sub-threshold residual (0 < level < 0.0005) above the engine's 0.00001 reset threshold; our speed-based getSprayerUsage makes that exact arithmetic vary run to run, which is the intermittency. Fix: in the appended onEndWorkAreaProcessing, complete the drain through the engine's own addFillUnitFillLevel when the tank is effectively empty (level < 0.001, type still set), so the engine's UNKNOWN reset fires and the AI stop works. In-game verification still pending.

## 2026-08-08 (Bob): SF-18 establishment failure built - the keystone lands
- [x] SF-18 ESTABLISHMENT FAILURE, the keystone of the spatial-soil drop. Seed that drowns during the establishment window becomes physically absent crop (growth-state 0 on the fruit's own plane), never a yield discount. `src/EstablishmentFailure.lua` implements the certified brief end to end: the ESTABLISHING window opened/extended by every sowing pass and closed only when the crop reaches first visible green (live FieldState sample); a daily threshold kill consuming SCS-018's positional `getMoisture(fieldId, x, z)` per zone cell, compaction-weighted, severity-scaled (Biological dial, neutral awaiting the spine); NO SIGNAL = NO THINNING on every absent path; contiguous waterlogged cells grouped into regions and killed once each through a verified in-mod substrate write (single DensityMapModifier polygon path, state 0, weed/spray clear); the base-game re-drill door with no invented gate.
- [x] Cadence: Time Guard `simulation`-class accrual at priority 95 (after the moisture store's 90, before stress/band refresh), with SF's own day tracking as the fallback and a frame-budgeted sweep pumped from `update()`. The old per-field `dailyKillCheck` wiring is gone; the sweep fires exactly once per day.
- [x] Bar: `establishment_window_spec_test.lua` at 25 assertions (brief certs 23): window machine, threshold/compaction/severity semantics, no-signal-no-thinning, kill-once, re-drill, live green close, positional per-cell kill with surviving cells keeping the window open, and the whole-stand close. Suite 2328/0 across 50 files; syntax and lint clean. Built and deployed. In-game verification still owed (the brief's state-0 look, dedicated-server propagation, frame cost at mass-sowing rollover).
- [x] The two prior session gaps were corrected during this build: `closeWindowIfEstablished` no longer closes unconditionally (which made the feature inert), and the write path no longer uses the unverifiable `updatePolygonWorldCoords` (the verified SoilLayerSystem polygon shape is used instead).

## 2026-08-10 (Fred): the Water Record read is published on the manager
- [x] SF-49's Water Record is now reachable across the mod boundary. `SoilFertilityManager:getWaterDaysInLast(days, throughDay)` returns `(count, known)` and delegates to `MaterialWetness:waterDaysInLast`, which was already built, working and unreadable: the only path ran three internal field names deep, which SoilFertilizer's own cross-boundary rule forbids. This is the third brief in as many days to name a read that turned out unreachable, so the read is published at the same manager boundary the SF-52 growth contract crosses (`g_currentMission.soilFertilityManager`).
- [x] The delegate is NEUTRAL, never a claim: nil means "we do not know", not "it was dry". A closed ground_material gate, a missing or unarmed wetness subsystem, a throwing read, and an empty record (`known == 0`) all return nil so the consumer falls back to its honest approximation instead of trusting a zero it did not earn.
- [x] `water_record_delegate_test.lua` at 12 assertions pinning the forward, the all-dry-is-known case, nil on every unknown path, the closed-gate case, and the untouched throughDay forwarding. Suite 2516/0 across 53 files; syntax and lint clean.
- [~] The consumer half is not this mod's to build: SeasonalCropStress's `getSkipRainHours` (SCS-037 round 2) goes live with no further change on their side when they call this delegate. In-game verification is theirs to run with the skip test.


## 2026-08-11 (Fred): SF-19 visibility parity - tooltip + client section fidelity
- [x] TOOLTIP PARITY (the spatial-visibility note's item 4, Arissani ruled required): `getFieldInfo(fieldId, x, z)` now reads pest, disease and compaction POSITIONALLY from the runtime value maps when a position is given, instead of returning the field scalars. The tooltip and the map now agree on the same per-cell truth. The disease discovery gate holds on the positional read: unscouted ground keeps `shownDiseasePressure` nil (the UI's "Unscouted"), the raw `diseasePressure` stays ungated for the NPC roll and scouting.
- [x] SEE-AND-SPRAY CLIENT RE-POINT (the note's item 5): the section decision in `installSeeAndSprayHook` now reads pest/disease from the SYNCED per-pixel display maps first (`HookManager.resolveCellPressure`), then the zoneData cell, then the field scalar. On a client the cell is the flattened copy, so this is what makes a client's section sprayer fire and skip where the host's would. Weed is untouched (the brief leaves it to the game-native weed density map).
- [x] `sf19_tooltip_parity_test.lua` at 11 assertions (positional wins, discovery gate on the positional read, field-scalar fallback) and `sf19_see_and_spray_repoint_test.lua` at 10 assertions (vm > cell > field preference, no-maps fallback, all-nil skip-safe). Suite 2537/0 across 55 files; syntax and lint clean.
- [~] In-game: scout an unscouted patch and confirm the PDA/tooltip reveals it at that position, and host/join MP to confirm the client's section sprayer fires where the host's does.

## 2026-08-11 (Fred): SF-23 spatial nutrients - the last un-flattening
- [x] SPATIAL NUTRIENTS (SF-23): the shipped leach weighed loss by SCS moisture x OM but at one number per field; this distributes that SAME field-level loss across 3-5 cached moisture bands so the wet hollow runs hungry and drifts acid while the dry knoll holds. New `src/SpatialNutrients.lua` implements the brief's build-now items: band assignment from the SCS positional read (`getMoisture(fieldId, x, z)`, one band = today's uniform leach when absent), per-tick banded accumulation into a per-band pending store, per-band hull flush via `applyDeltaToPolygon` with the floor rule (settle against the RETURNED shift), tier-0 texture (field-level soil type from SCS, loam fallback for the F157 gap), pH banded with the leach, harvest depletion banded (P included: crop removal is one of its two spatial pathways), the spine Agronomy multiplier (neutral 1.0 when absent), and the reciprocal `getSoilValueAtWorld` read for SCS.
- [x] Wired server-side into the leach block (`applyRainEffects`), the harvest depletion block (`updateFieldNutrients`), and the daily cadence (`updateDailySoil` band refresh). Field-level model byte-for-byte unchanged; phosphorus leach stays field-level (it binds), its spatial moves are application and removal.
- [x] `sf23_spatial_nutrients_test.lua` at 13 assertions: absent-SCS one-band fallback, wet/dry banding, CONSERVATION (band shares sum exactly to the field loss), the floor rule (un-applied remainder kept), texture loam fallback, convex hull, and the reciprocal read. Suite 2550/0 across 56 files; syntax and lint clean.
- [~] In-game (owed): banded flush frame cost at map scale, applyDeltaToPolygon hull behaviour at map edges, and the scout-visible wet/dry nutrient picture. The tier-1/2 texture surfaces and SCS consumption of the reciprocal read are maturity-asks, neutral when absent.

## 2026-08-11 (Fred): SF-21 neighbour crossing - the hedge stops being a wall
- [x] NEIGHBOUR CROSSING (SF-21): what arrives at a field's edge now depends on what is actually across it. New `src/NeighbourCrossing.lua` upgrades the SF-19 edge mechanism (the reserved slot). The crossing PRE-PASS (B1) runs once per day ahead of the mutation batches (the completion gate is LAW), sampling a short distance past each field's boundary and storing a transient per-day neighbour-pressure snapshot (never saved, never synced). B2 recomposes the pest origin weight as baseline x neighbour modifier (1.0 for nil/zero, bounded monotonic; a zero-pressure district is byte-identical to today). B3 rolls a conducive-gated disease crossing: where the snapshot shows infected ground across an arc AND boundary moisture is above threshold (the SCS read, pcall, absent = no disease crossing), a success seeds a boundary-cell origin through the pressure build's own entry (SpatialPressures:seedBoundaryOrigin), with the protection fence LAW (a protected field, fungicideDaysLeft > 0, seeds nothing). B4 constants are conservative and tunable.
- [x] Wired server-side: the pre-pass in updateDailySoil, the disease roll and pest recomposition in the per-field daily pressure path, all behind the spatial_soil release gate.
- [x] `neighbour_crossing_spec_test.lua` at 12 assertions: the composition/degrade bar (1.0 for nil/zero), the completion gate, the protection fence, absent-SCS no-crossing, the wilderness form, and the dry-boundary gate. Suite 2549/0 across 56 files; syntax and lint clean.
- [~] In-game (owed): a high-pressure field visibly biasing its neighbour's arrival arcs over several days; a clean district behaving exactly like the pressure build alone; treating a boundary holding for the full protection window; no-moisture-mod (pest crossing works, disease silently off); pre-pass frame cost at district scale.

## 2026-08-11 (Fred): SF-27 NPC soil - NPC-managed ground joins the soil sim
- [x] NPC SOIL (SF-27, LANE B): NPC-managed farmland joins the daily soil simulation exactly like owned land. New `src/NpcSoilBridge.lua` is SF's side of the cross-mod contract: it reads NPCFavor's designation surface (`isNPCManaged` / `getWorkingState`) through the mission handle (pcall, neutral-absent), publishes the phase-2 capability marker (`npcSoilPhase2`) NPCFavor probes, and provides the fail-closed attribution predicate (an operation on designated ground is NPC-ATTRIBUTED, never the player's bill).
- [x] Membership widened (owned OR NPC-managed) at the primary scan, secondary scan, the client active-set rebuild, and the leave-path (a designated field stays active on farm release). B5: designated ground is skipped by rerollUnownedFields. The named-fungicide treatment charge is gated on the attribution predicate (NPC-attributed treatments never move player money).
- [x] NPCFavor side: `isNPCManaged` / `getWorkingState` / `getNPCForFarmland` published on NPCFavorSystem (from the assignedFarmland records), and the ownership flip targets a GUARDED real farm id (never getFarmId() on a dedicated server, never or-fallback past a possible 0).
- [x] `npc_soil_gate_spec_test.lua` at 16 assertions (owned/NPC truth table, fail-closed attribution, neutral-when-absent, throwing-surface pcall) and `npc_soil_designation_spec_test.lua` at 8 assertions. Suite 2586/0 across 59 files; syntax and lint clean.
- [~] In-game (owed): an NPC-designated unowned field survives a daily pass, a reroll run and save/load with state intact and painted; an NPC job runs end to end with zero player money movement; buying the parcel inherits its history; a joined MP client paints NPC ground after full sync.

## 2026-08-14 (Fred): RSF-836 the swept quad painted a third to a half of the boom
- [x] ROOT CAUSE: paintBoomStrip read the boom line off the ends of the cell sweep array, whose last element is always the vehicle root and whose extent is a world-axis projection, so it painted one tip to the middle of the machine, halved and foreshortened by the heading cosine (36.6 m boom: 18.30 m square, 12.94 m at 45 deg). Mass was conserved, so the covered band ran at 2x to 2.8x the rate and the rest of the boom took nothing.
- [x] FIX (items 1-4 of the RSF-836 fix doc): the true endpoints are derived like the engine derives a working width (WorkArea.lua:307-314), every collected node transformed into components[1].node, min and max local X are the tips. The fallback lays the working-width half span on the frame's local X. paintBoomStrip consumes the line, never the array ends, with a tip-swap guard. markBoomCells and getBoomCellPositions untouched (cell stamping byte-identical); the partial-width VWW exclusion inherited; the teleport guard widens back.
- [x] Item 5 (the ~500 ms anchor drop) split out as a design item, NOT in this fix.
- [x] rsf836_boom_line_test.lua at 11 assertions (0/30/45 deg main path, fallback width, partial-width exclusion, the old array-end read as the RED case). Suite 2886/0 across 69 files; syntax and lint clean.
- [~] In-game (owed): a wide boom on a diagonal pass, ground read after one pass.

## 2026-08-14 (Fred): organic transition normalized to YEARS via Time Guard
- [x] The transition term was a raw day count (60/120/240), so 'three years' meant anything from months to decades depending on days-per-month. It is now TRANSITION_YEARS (2/3/5 indicative, values ride the balance pass), resolved at read time through Time Guard's days-per-period. Mid-save days-per-period changes re-normalise from the next period; Time Guard absent degrades to the 30-day reference. State machine and getFieldOrganicState shape unchanged.
- [x] organic_transition_timeguard_test.lua at 11 assertions; suite 2897/0; deployed 2.5.0.71.
- [~] In-game (owed): a transition at a 30-day month counts down over three in-game years, not eight months.

## 2026-08-14 (Fred): organic compost production, the managed process
- [x] A managed compost process (no placeable): batches commit farm organic waste, decompose over in-game days on the Time Guard clock (SF day-tracking fallback when absent), and yield the existing COMPOST fill type into farm storage. SF owns the OM effect unchanged; organic-safe flag per feedstock (biosolids-fed is not cert-safe, breach rides onInputApplied). Server-authoritative, StateLedger + own-XML persistence, sfCompostStatus/Start/Collect console.
- [x] compost_manager_test.lua at 25 assertions; suite 2922/0; deployed 2.5.0.72.
- [~] In-game (owed): start a batch, watch it decompose across days, collect the compost, spread it on a field.

## 2026-08-14 (Fred): SF-14 zone yield of the established crop - the payout half of the 2m family
- [x] ZONE YIELD CAPTURE (per cell, at growth time): the harvest no longer pays one field-average number. A new `src/ZoneYield.lua` rides the family's shared read (`ViabilityMask:getCellGrowthInfo`, now extended to carry the full input set: N/P/K + compaction + moisture), scores each cell's own N/P/K through the SAME single source of truth computeYieldModifier uses (`_yieldModifierFromNutrients`), clamps to the efficiency band (0.7 to 1.15, AWAITING-SPINE neutral default), and writes the captured truth to the `yieldEfficiency` value-map layer (maxVal widened 100 to 115 to hold the band ceiling). The display-only field-average stamp is retired: the mirror and seed stand down from `yieldEfficiency` while the capture is live, so the DMV shows the captured truth.
- [x] THE FREEZE SUPERSESSION (harvest read): on the spatial path (maps present) the SF-16 scalar `frozenYieldModifier` freeze is BYPASSED. The harvest hook reads the captured, growth-time-static layer per cell across the header (work-area polygons, bounded 4 m grid) and applies the area-weighted positional integral (SF-25's ratified rule) as the per-pass modifier, computed fresh each pass. When the spatial read cannot answer (maps absent, not live, or no captured data) the call falls through to the untouched field-average `computeYieldModifier` with its scalar freeze, one branch, untouched.
- [x] THE CALIBRATION INVARIANT, bench-asserted: a uniform untrafficked field's area-weighted read reconciles against computeYieldModifier output for the same inputs, because the capture formula IS that same formula evaluated per cell; the band's floor/ceiling bounds the named residual divergence. SF-55's drag composition line (effective = captured x (1 - drag)) is applied from day one, nil drag reading as zero.
- [x] `zone_yield_sf14_test.lua` at 37 assertions: band defaults locked, per-cell reconciliation vs computeYieldModifier on uniform in-band fields, band floor/ceiling clamps, SF-25 area-weighted aggregation, header polygon sampling + area-weighted read, neutral flows (not live / no maps / no geometry / empty header -> nil fallback), the live gate, and the capturedEfficiency socket through ViabilityMask. Suite 2956/0 across 73 files (the om_213 load failure in a temp clone is its sibling-dir dependency, absent here); syntax and lint clean. Deployed 2.5.0.77.
- [~] In-game (owed): a header crossing a rich patch and a poor patch pays per patch; a uniform field reconciles with the pre-family harvest; save/reload mid-harvest re-reads the static layer; harvest-read cost at full header width (a named bench item, not asserted).

